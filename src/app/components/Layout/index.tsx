import React, { Fragment, ReactNode } from 'react';
import Navigation from '../Navigation'
  
const Layout= () => {
  return (
    <Fragment>
        <Navigation/>
    </Fragment>
  )
}

export default Layout